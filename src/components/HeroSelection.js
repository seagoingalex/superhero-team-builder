import React from "react";
import HeroCard from "./HeroCard";


function HeroSelection() {
  return (
    <div className="heroSelectionContainer">
        <h1 style={{color: "red"}}>this is HeroSelection</h1>
        <HeroCard />

    </div>
  );
}

export default HeroSelection;
